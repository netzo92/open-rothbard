# Scout worker — lightweight opportunity scanner
# Used when the orchestrator wants to run parallel market scans

FROM python:3.12-slim

WORKDIR /scout

RUN pip install --no-cache-dir httpx

COPY scout_entrypoint.py .

ENV SCAN_TARGET=""  # defi|freelance|arbitrage|content

CMD ["python", "scout_entrypoint.py"]
